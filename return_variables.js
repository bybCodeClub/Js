// Getting a return value from a function and putting it in a variable 


var Return_value_reassigned = null 


function Let_add_numbers(num, num2) {
    return (num + num2 + 33)  
}

// To store this function value in a variable;

Return_value_reassigned = document.write(Let_add_numbers(3,8))

// Here were assigning the return_value_re*** variable to the let's_add_number function call  